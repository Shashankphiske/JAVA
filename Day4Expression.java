public class Day4Expression {
    public static void main(String[] args)
    {
        //expression = operands and operator
        //operands = data
        //operators = /, +, - etc

        int a = 9;
        double b;

        b = (double) 9/2; // here the output of 9/2 will be 4 because both values used are integers
                          // so we will cast the output into double value

        System.out.println(b);

    }
}
